# Warehouse City 9.1 사용 안내

실행 주소: https://dalmook.github.io/3D_warehouse/

이전의 `city.html` 미리보기 대신 `index.html`이 새 기본 화면입니다. `city.html` 주소도 기본 화면으로 연결됩니다. 구버전 정밀 편집기는 `classic.html`에 남겨 두었습니다.

## 설계

상단 **01 설계**에서 예제 도면으로 시작하거나 빈 창고를 만듭니다. 설비 목록에서 랙·팔레트·작업대 등을 고른 뒤 바닥을 클릭하면 격자에 맞추어 놓입니다. 선택 모드에서는 설비를 드래그하고 오른쪽 설정에서 위치·크기·회전·색상을 조정합니다. 랙은 베이와 단수를 조정할 수 있습니다. 일괄 배치는 전체 후보 위치를 검사한 뒤 한 번에 적용하므로 실패한 일부만 남지 않습니다. 실수는 실행 취소/다시 실행으로 복구하세요.

모바일에서는 `설비`, `설정` 버튼으로 패널을 열고 닫습니다. 넓게 보기로 3D 화면에 집중할 수 있습니다. 세밀한 대규모 배치는 PC가 편합니다.

## 운영 시뮬레이션

작업자를 놓고 방문할 작업 지점을 배치합니다. **02 운영 → 실행**을 누르면 작업자가 작업 지점의 배치 순서를 반복 방문합니다. 위치 이름의 숫자를 바꾸는 것은 순서 변경 기능이 아닙니다. 속도·대기시간·배속을 조정하고 이동 거리·목적지 도착·경로 오류를 확인합니다.

작업자가 움직이지 않을 때는 작업자와 작업 지점이 모두 있는지, 출발점이 랙 내부가 아닌지, 도달 가능한 통로가 있는지 확인하세요. 통로를 막으면 경로를 찾지 못할 수 있습니다. 실행 중 위치는 모의 위치이며 설계상의 원래 좌표를 변경하지 않습니다.

## 1인칭 내부 체험

**03 내부 체험**을 선택합니다. 운영 실행 상태를 유지하면 직접 걷는 동안에도 작업자가 움직입니다.

- PC: WASD/방향키 이동, Shift 빠르게 이동, 마우스 드래그 시점 변경. 마우스 잠금은 사용자 클릭으로 시작하고 Esc로 해제합니다.
- 휴대폰: 화면의 방향 버튼을 누르고 있으면 이동합니다. 화면을 드래그해 주변을 봅니다.
- 랙·벽·창고 경계는 통과하지 못합니다. 방 형태 설비는 외곽을 고체 장애물로 취급하며 방 안의 출입 동작은 제공하지 않습니다.

## 저장과 다른 기기에서 열기

브라우저 자동 저장은 현재 기기·브라우저에만 남습니다. 다른 기기로 가져가려면 GitHub 저장 또는 JSON 파일을 이용합니다.

**GitHub 저장** 창에서 소유자·저장소·브랜치·`layouts/내도면.json` 경로를 지정합니다. 저장 권한이 있는 Fine-grained PAT를 입력하고 공개 저장 관련 안내를 확인한 후 저장하세요. 토큰에는 대상 저장소의 `Contents: Read and write` 권한이 필요합니다. 토큰을 코드나 도면에 넣지 마세요. 창을 닫으면 입력된 토큰은 지워집니다.

다른 기기에서는 같은 저장소·브랜치·경로를 지정하거나 파일 목록에서 선택해 불러옵니다. 비공개 저장소 조회에는 읽기 권한도 필요합니다. 현재 저장소는 공개되어 있으므로 회사 기밀 도면은 여기에 올리지 마세요.

충돌 안내가 나오면 현재 작업을 JSON으로 내보낸 뒤 최신 도면을 불러와 비교하세요. 자동으로 다른 사람의 변경사항을 덮어쓰지 않습니다. 오프라인에서는 브라우저/파일 저장을 이용하고 GitHub 저장은 연결 후 수행합니다.

## 기존 자료와 지원 범위

구버전 JSON을 새 화면에 불러올 수 있지만 지원되지 않는 전문 설비는 단순화될 수 있습니다. 원본 파일은 보관하고 정밀 편집이 필요하면 `classic.html`을 사용하세요. 기존 저장소의 `layouts/`와 구버전 localStorage 저장 키는 새 화면이 자동 덮어쓰지 않습니다.

이 앱은 동선을 이해하고 배치를 검토하는 도구입니다. 실제 작업자 추적, 다중 사용자 동시 편집, 작업자 간 충돌·대기열, 산업용 물류 처리량 예측이나 안전 인증은 제공하지 않습니다. 대규모 도면 성능은 사용하는 기기의 GPU에 따라 달라집니다.
