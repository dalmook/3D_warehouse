# Warehouse City v9

`city.html`은 기존 편집기와 독립적으로 동작하는 새 창고 설계·동선 체험 화면입니다. 기존 `index.html`, `app.js`, `objects.js`와 기존 브라우저 도면은 삭제하지 않습니다.

## 사용 순서

1. **설계**: 왼쪽 설비를 선택하고 바닥을 클릭합니다. 배치 전 초록/빨강 미리보기로 위치를 확인합니다. R은 90도 회전, Esc는 배치 취소입니다. 선택한 설비를 드래그하거나 오른쪽에서 위치·크기·높이를 입력할 수 있습니다. 기본 격자는 0.5m입니다.
2. **빠른 설계**: 표준 물류센터 템플릿, 빈 창고, 랙 한 줄 자동 배치를 이용합니다. 실행 취소/다시 실행은 최대 60단계를 보관합니다.
3. **운영**: 설계에서 작업자와 이동 목적지를 배치한 다음 운영 탭에서 시작합니다. 작업자는 배치된 목적지를 순서대로 방문하며 정지 시간을 갖습니다. 1/2/4배속과 경로 표시를 지원합니다. 설비를 통과하는 경로는 허용하지 않으며, 도달할 수 없으면 경로 오류를 표시합니다.
4. **내부 체험**: WASD 또는 방향키로 이동하고, 화면을 드래그하거나 마우스 잠금 버튼으로 둘러봅니다. Shift는 빠르게 이동, Esc는 마우스 잠금 해제입니다. 휴대폰은 화면 방향 버튼과 드래그를 이용합니다.
5. **도면 보관**: 브라우저 자동 저장은 현재 브라우저에만 남습니다. 중요한 도면은 JSON 내보내기 또는 GitHub 저장을 별도로 실행하세요.

## 서버 없는 GitHub 저장

웹 서버 프로그램, 데이터베이스, S3, Supabase, Cloudflare Worker를 요구하지 않습니다. GitHub Pages에서 정적 파일을 제공하고, 도면은 GitHub REST Contents API로 JSON 커밋을 생성합니다. 개발·검증용 Node.js는 방문자의 PC나 운영 서버에 설치할 필요가 없습니다.

- 저장 버튼에서 소유자·저장소·브랜치·`layouts/영문이름.json` 경로를 지정합니다.
- 저장 권한이 있는 사용자가 해당 저장소만 선택한 fine-grained personal access token을 입력합니다. 필요한 저장 권한은 **Contents: Read and write**입니다.
- 토큰은 프런트엔드 소스에 하드코딩하지 않으며 localStorage, sessionStorage, URL, 내보낸 도면에 기록하지 않습니다. 저장 창을 닫으면 입력값을 지웁니다. 열려 있는 페이지나 악성 브라우저 확장 프로그램의 접근 위험까지 없애는 방식은 아니므로 신뢰할 수 있는 PC/브라우저를 사용하세요.
- 도면을 열거나 저장할 때 받은 파일 SHA와 최신 SHA를 비교합니다. 다른 사용자가 변경한 경우 자동 덮어쓰지 않고 중단합니다. 처음 접근하는 같은 이름 파일은 덮어쓰기 확인을 받습니다.
- 동시 편집·자동 병합·멀티플레이는 지원하지 않습니다. 서로 다른 파일명으로 저장하면 사용자별 설계안을 구분할 수 있습니다.
- 도면 목록은 `layouts/` 바로 아래 JSON 파일을 조회합니다. 새 도면에 `layouts/index.json` 수동 등록은 필요하지 않습니다.

**공개 저장소의 도면은 공개됩니다. 실제 회사·보안시설 도면은 공개 저장소에 저장하지 마세요.** 앱을 공개 Pages에서 제공하더라도 도면 저장 대상을 별도 비공개 저장소로 바꿀 수 있습니다. 비공개 도면 조회에는 해당 저장소 읽기 권한도 필요합니다.

## 기존 도면과 호환성

새 브라우저 저장 키는 `warehouse-city-v9`, 기존 키는 `warehouse-studio-project-v1`입니다. 기존 도면 가져오기 버튼은 기존 데이터를 읽을 뿐 덮어쓰지 않습니다. JSON 내보내기 후 가져오기 방식도 사용할 수 있습니다.

가로·세로·높이, 이름, 위치, 회전, 잠금, 랙 설정 등은 보존합니다. 새 카탈로그에 없는 기존 전용 설비는 치수를 유지하는 단순 블록으로 표시됩니다. 기존 V8의 박스 자동 적재와 일부 특수 설비 상세 모델을 동일하게 구현한 것은 아닙니다. 원본 도면을 별도로 보관하세요.

## 범위와 한계

- 운영 모드는 **작업자 동선 시각화**입니다. 실제 생산성·출고량 예측, 사람 간 충돌/양보, 작업 대기열, 지게차 자동 운행, 실시간 작업자 위치 연동은 포함하지 않습니다. 표시하는 완료 횟수는 목적지 도착 횟수입니다.
- 1인칭 체험은 보행형 카메라입니다. 총기·전투 기능이나 실제 Doom/SimCity 자산을 사용하지 않습니다.
- 벽과 설비는 평면 장애물로 계산하며 방 형태 시설은 외곽 전체를 장애물로 취급합니다. 통과 가능한 출입문 객체가 방 외곽에 자동으로 구멍을 만들지는 않습니다.
- 편집은 창고 10~200m × 10~200m, 높이 3~30m, 최대 600개 객체를 제한으로 둡니다. 시뮬레이션은 작업자 40명까지입니다. 최대 규모에서 모든 기기의 성능을 보증하지 않습니다.
- 적재 하중, 구조·소방·피난 기준 준수 여부를 판정하지 않습니다. 실제 배치 확정에는 전문 검토가 필요합니다.

## 검증

`node --test city-core.test.mjs`로 데이터 변환, 설비 겹침·경계, 회전 장애물, 얇은 벽 우회, 막힌 경로, 작업자 이동, 실행 취소, 한글 JSON 인코딩을 검사합니다. GitHub Actions는 핵심 테스트와 앱·Three.js 모듈 문법을 검사합니다.

브라우저 렌더링 검증과 실제 GitHub 인증 저장 검증은 핵심 단위 테스트와 별개입니다. 검증 결과는 커밋 및 Actions 결과를 기준으로 확인하세요.
