# Warehouse City 9.1.1

## 마지막 코드 검토 반영

랙의 반복 부품을 개별 Mesh 대신 InstancedMesh로 표시합니다. 전체 랙의 예상 부품 수가 12,000개를 넘으면 기둥과 선반 중심의 간소화된 표현을 사용하고 화면에 안내합니다. **저장된 베이 수·단수·용량·설비 크기·위치·충돌 계산은 바뀌지 않습니다.** 도면을 줄이면 상세 표시로 돌아옵니다. 인스턴스 GPU 자원은 다시 만들거나 배치 미리보기를 종료할 때 해제합니다.

GitHub Actions 게시 방식에서도 `layouts/`만 변경한 커밋이 배포 작업을 실행하도록 제외 조건을 수정했습니다. GitHub 저장은 여전히 별도 서버 없이 Contents API를 직접 사용합니다. 기존 main/root Pages 설정은 유지합니다.

## 추가 검증

- 핵심 테스트: `node --test city-core.test.mjs tests/*.test.mjs`
- 기존 데스크톱/모바일 22개 시나리오: `node tests/browser.mjs`
- 최대 구성(20베이×20단) 랙 600개: `node tests/stress.mjs`
- 대용량 도면 표시, 원본 JSON 왕복, 반복 재생성, 선택/복제 및 작업자 시뮬레이션 복귀를 확인합니다.
- 기존 편집기와 실제 GitHub 생성·수정·불러오기 검사를 유지합니다.
- 공개 파일의 소스 해시 및 공개 사이트에서 동일 검사를 통과한 실행만 배포 검증 완료로 판단합니다.

테스트 결과는 GitHub Actions의 실제 실행 결과와 QA 자료가 기준입니다. 모바일은 Chromium 터치 에뮬레이션이며 모든 휴대폰 실기기나 GPU의 성능을 보장하는 검사는 아닙니다.
